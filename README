test プロジェクトは各クラスの単体テストの集まりのプロジェクトです。
実行するためには Flex 4 の SDK と AIR SDK 2.0 が必要です。
Flex 4 に対応した FlexUnit4 が予めバンドルされています。

Flash Builder を使ってテストを実行するには予め変数を定義しておく必要があります。
「一般」->「ワークスペース」->「リンクされたリソース」

* GP_COMPAT_PATH
compat へのパス。SVN 上では /as3/gunyarapaint/branches/gunyarapaint/compat

* GP_FRAMEWORK_PATH
framework へのパス。SVN 上では /as3/gunyarapaint/branches/gunyarapaint/framework

FlashDevelopの場合は「環境設定」 -> 「AS3 Context」 -> 「Flex SDK Location」 を
Flex 4.0 の SDK があるパスに指定する必要があります。
